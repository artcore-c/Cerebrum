# .env example
#
# Cerebrum CM4 Example Configuration
# ------------------------------------------------------------
# This file runs on the CM4 (uConsole / Pi / edge device)
# It connects OUT to the Cerebrum VPS backend.
#
# Copy this file to .env and adjust values as needed.

# ------------------------------------------------------------
# Backend connection
# ------------------------------------------------------------
# URL of the Cerebrum VPS backend.
# This may be:
# - http://127.0.0.1:9000       SSH reverse tunnel (Recommended)
# - http://100.x.y.z:9000       Tailscale (This can work but may interfere if using SSH with Tailscale)
# - https://your-domain:9000    Public VPS IP, if exposed (Not recommended)
#
VPS_ENDPOINT=http://127.0.0.1:9000

# API key shared with the VPS backend
# Must match CEREBRUM_API_KEY generated on the VPS
VPS_API_KEY=your-api-key-here

# ------------------------------------------------------------
# Local CM4 server settings
# ------------------------------------------------------------
# Address the CM4 frontend binds to
# 0.0.0.0 = listen on all interfaces (Recommended)
CEREBRUM_HOST=0.0.0.0

# Port the CM4 frontend listens on
CEREBRUM_PORT=7000

# ------------------------------------------------------------
# Logging
# ------------------------------------------------------------
# DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
